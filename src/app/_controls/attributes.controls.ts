import { FormControl, FormBuilder, FormGroup, FormArray, Validators } from '@angular/forms';


export const AttributesControls = {
                    bodyBackground: [''],
                    containerBackground: [''],
                    heroColour : [''],
                    secondaryColour : [''],
                    globalWidth: [''],
                };
